import { Meta, Story } from '@storybook/react';

import { ToolbarLinks } from '~/components/toolbar-links';

export const Default: Story = () => {
  return <ToolbarLinks />;
};

export default {
  title: 'Components/Toolbar links',
} as Meta;
